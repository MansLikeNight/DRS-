# Django Configuration# Copy this file to .env and fill in your values

SECRET_KEY=your-secret-key-here-change-in-production# DO NOT commit .env to version control!

DEBUG=True

ALLOWED_HOSTS=localhost,127.0.0.1SECRET_KEY=your-secret-key-here-generate-new-one

DEBUG=False

# Database Configuration (Development - SQLite)ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

DB_ENGINE=django.db.backends.sqlite3

DB_NAME=db.sqlite3# Database (for production PostgreSQL)

# DATABASE_URL=postgresql://user:password@localhost:5432/drillreport

# Database Configuration (Production - PostgreSQL)

# Uncomment and configure for production deployment# Email settings (for production)

# DB_ENGINE=django.db.backends.postgresqlEMAIL_HOST=smtp.gmail.com

# DB_NAME=dailydrillreport_dbEMAIL_PORT=587

# DB_USER=your_db_userEMAIL_USE_TLS=True

# DB_PASSWORD=your_db_passwordEMAIL_HOST_USER=your-email@gmail.com

# DB_HOST=localhostEMAIL_HOST_PASSWORD=your-app-password

# DB_PORT=5432

# Or use DATABASE_URL for platforms like Render, Railway, Heroku
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Email Configuration (Optional)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
